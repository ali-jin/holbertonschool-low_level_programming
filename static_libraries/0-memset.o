#include <stdio.h>
#include "main.h"

/**
 * _memset - Entry point
 * @s: pointer
 * @b: character
 * @n: unsigned integer
 *
 * Return: Always s (Success)
 */
char *_memset(char *s, char b, unsigned int n)
{
	char c = b;
	char *ch;

	for (ch = s; n > 0; ++ch, --n)
		*ch = c;
	return (s);
}
